@model eEvento.Models.Inscricao

@{
    ViewBag.Title = "Excluir Inscrição";
}

<div class="container mt-4">
    <h2 class="text-danger">Excluir Inscrição</h2>
    <h3>Tem certeza de que deseja excluir esta inscrição?</h3>

    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Detalhes da Inscrição</h4>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Evento.Nome)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Evento.Nome)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Participante.Nome)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Participante.Nome)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.DataInscricao)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.DataInscricao)</dd>
            </dl>
        </div>
    </div>

    <div class="mt-3">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-actions">
                <input type="submit" value="Excluir" class="btn btn-danger me-2" />
                @Html.ActionLink("Voltar para a lista", "Index", null, new { @class = "btn btn-secondary" })
            </div>
        }
    </div>
</div>
