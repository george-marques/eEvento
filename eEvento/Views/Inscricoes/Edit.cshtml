@model eEvento.Models.Inscricao

@{
    ViewBag.Title = "Editar Inscrição";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">@ViewBag.Title</h4>
                </div>
                <div class="card-body">
                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-horizontal">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger mb-3" })
                            @Html.HiddenFor(model => model.InscricaoId)

                            <div class="mb-3">
                                <strong>@Html.LabelFor(model => model.DataInscricao, htmlAttributes: new { @class = "control-label" })</strong>
                                @Html.EditorFor(model => model.DataInscricao, new { htmlAttributes = new { @class = "form-control", @Value = Model.DataInscricao.ToString("yyyy-MM-dd") } })
                                @Html.ValidationMessageFor(model => model.DataInscricao, "", new { @class = "text-danger" })
                            </div>

                            <div class="mb-3">
                                <strong>@Html.LabelFor(model => model.EventoId, "Evento", htmlAttributes: new { @class = "control-label" })</strong>
                                @Html.DropDownList("EventoId", new SelectList(ViewBag.Eventos, "EventoId", "Nome", Model.EventoId), "Selecione um evento", htmlAttributes: new { @class = "form-control" })
                                @Html.ValidationMessageFor(model => model.EventoId, "", new { @class = "text-danger" })
                            </div>

                            <div class="mb-3">
                                <strong>@Html.LabelFor(model => model.ParticipanteId, "Participante", htmlAttributes: new { @class = "control-label" })</strong>
                                @Html.DropDownList("ParticipanteId", new SelectList(ViewBag.Participantes, "ParticipanteId", "Nome", Model.ParticipanteId), "Selecione um local", htmlAttributes: new { @class = "form-control" })
                                @Html.ValidationMessageFor(model => model.ParticipanteId, "", new { @class = "text-danger" })
                            </div>

                            <div class="d-flex justify-content-end mt-4">
                                <a href="@Url.Action("Index")" class="btn btn-secondary me-2">
                                    <i class="bi bi-arrow-left-circle"></i> Voltar
                                </a>
                                <button type="submit" class="btn btn-success">
                                    <i class="bi bi-save"></i> Salvar
                                </button>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
