@model eEvento.Models.Participante

@{
    ViewBag.Title = "Excluir Participante";
}

<div class="container mt-4">
    <h2 class="text-danger">Excluir Participante</h2>
    <h3>Tem certeza de que deseja excluir este participante?</h3>

    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Detalhes do Participante</h4>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Nome)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Nome)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Email)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Email)</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.CPF)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.CPF)</dd>
            </dl>
        </div>
    </div>

    <div class="mt-3">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-actions">
                <input type="submit" value="Excluir" class="btn btn-danger me-2" />
                @Html.ActionLink("Voltar para a lista", "Index", null, new { @class = "btn btn-secondary" })
            </div>
        }
    </div>
</div>
